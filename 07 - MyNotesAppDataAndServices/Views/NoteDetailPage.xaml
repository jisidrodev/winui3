<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="MyNotesApp.Views.NoteDetailPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:MyNotesApp.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.Resources>
        <Style x:Key="AttributeTitleStyle" TargetType="TextBlock">
            <Setter Property="HorizontalAlignment" Value="Right" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
        <Style x:Key="AttributeValueStyle" TargetType="TextBox">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="8" />
        </Style>
        <Style x:Key="AttributeComboBoxValueStyle" TargetType="ComboBox">
            <Setter Property="HorizontalAlignment" Value="Stretch" />
            <Setter Property="Margin" Value="8" />
        </Style>
    </Page.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock
            Margin="8"
            FontSize="18"
            Text="Note Details" />
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="200" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <TextBlock
                Grid.Row="0"
                Grid.Column="0"
                Style="{StaticResource AttributeTitleStyle}"
                Text="Title:" />
            <TextBox
                Grid.Column="1"
                Style="{StaticResource AttributeValueStyle}"
                Text="{x:Bind ViewModel.Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Style="{StaticResource AttributeTitleStyle}"
                Text="Content:" />
            <TextBox
                Grid.Row="1"
                Grid.Column="1"
                Style="{StaticResource AttributeValueStyle}"
                Text="{x:Bind ViewModel.Content, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <TextBlock
                Grid.Row="2"
                Grid.Column="0"
                Style="{StaticResource AttributeTitleStyle}"
                Text="NoteType" />
            <ComboBox
                Grid.Row="2"
                Grid.Column="1"
                ItemsSource="{x:Bind ViewModel.NoteTypes}"
                SelectedValue="{x:Bind ViewModel.SelectedNoteType, Mode=TwoWay}"
                Style="{StaticResource AttributeComboBoxValueStyle}" />
        </Grid>
        <StackPanel
            Grid.Row="2"
            HorizontalAlignment="Right"
            Orientation="Horizontal">

            <SplitButton
                x:Name="SaveButton"
                Margin="8,8,0,8"
                Command="{x:Bind ViewModel.SaveNoteAndReturnCommand}"
                Content="Save and Return"
                IsEnabled="{x:Bind ViewModel.IsDirty, Mode=OneWay}">
                <SplitButton.Flyout>
                    <Flyout>
                        <StackPanel>
                            <Button
                                Background="Transparent"
                                Command="{x:Bind ViewModel.SaveNoteAndContinueCommand}"
                                Content="Save and Create New"
                                IsEnabled="{x:Bind ViewModel.IsDirty, Mode=OneWay}" />
                            <Button
                                Background="Transparent"
                                Command="{x:Bind ViewModel.SaveNoteAndReturnCommand}"
                                Content="Save and Return"
                                IsEnabled="{x:Bind ViewModel.IsDirty, Mode=OneWay}" />
                        </StackPanel>
                    </Flyout>
                </SplitButton.Flyout>
                <SplitButton.Resources>
                    <TeachingTip
                        x:Name="SavingTip"
                        Title="Save and create new"
                        Subtitle="Use the dropdown button option to save your item and create another."
                        Target="{x:Bind SaveButton}" />
                </SplitButton.Resources>
            </SplitButton>
            <Button
                Margin="8"
                Command="{x:Bind ViewModel.CancelCommand}"
                Content="Cancel" />
        </StackPanel>
    </Grid>
</Page>
